TOKEN = 'token ID'
ADMIN_CHAT_ID = '123456' # your_target_chat_ID
SHORT_LINK = '@your_short_channel_link'
LINK = 'https://your_channel_link'
IP = '123.123.123.123' # your or server IP addr
ADVERTISING_LINK = 'your link to the advertising post'
CONTENT_IGNORE_TYPES = ['audio', 'document', 'photo', 'sticker', 'video',
		        'video_note', 'voice', 'location', 'contact']
BUTTON_NAMES = {
    'name': 'description',
}
INFO = {
    'command name': (
        'text'
    ),
}

# config for webhook use cherrypy
WEBHOOK_HOST = IP
WEBHOOK_PORT = 8443 # 443, 80, 88 or 8443 (port need to be 'open')
WEBHOOK_LISTEN = '0.0.0.0' # In some VPS you may need to put here the IP addr

WEBHOOK_SSL_CERT = './webhook_cert.pem' # Path to the ssl certificate
WEBHOOK_SSL_PRIV = './webhook_pkey.pem' # Path to the ssl private key

WEBHOOK_URL_BASE = 'https://%s:%s' % (WEBHOOK_HOST, WEBHOOK_PORT)
WEBHOOK_URL_PATH = '/%s/' % (TOKEN)
